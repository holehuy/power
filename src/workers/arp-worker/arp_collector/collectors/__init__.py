"""1 module / vendor, ứng với đúng 1 giá trị của ArpDeviceStatus.DeviceType (7.3).

Thêm vendor mới: thêm 1 file ở đây implement collect(device) -> list[ArpEntry],
đăng ký vào DEVICE_TYPE_DISPATCH trong main.py — KHÔNG cần sửa các collector khác.
"""
